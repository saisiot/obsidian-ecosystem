# ============================================
# Obsidian RAG MCP 환경 변수 설정
# ============================================
#
# 이 파일을 복사해서 `.env` 파일을 만들고
# 아래 값들을 여러분의 환경에 맞게 수정하세요.
#
# 복사 방법:
#   cp .env.example .env
#
# ============================================

# Obsidian Vault 경로 (필수)
# 여러분의 Obsidian Vault가 있는 폴더의 절대 경로
#
# 확인 방법:
#   1. Finder에서 Vault 폴더 우클릭
#   2. "Option" 키를 누른 상태에서 "... 경로 이름 복사" 선택
#   3. 아래에 붙여넣기
#
# 예시:
#   macOS: /Users/yourname/Documents/SecondBrain
#   Windows: C:/Users/yourname/Documents/SecondBrain
#   Linux: /home/yourname/Documents/SecondBrain
#
OBSIDIAN_VAULT_PATH=/Users/yourname/Desktop/SecondBrain

# ============================================
# 선택 사항 (고급 사용자용)
# ============================================

# 데이터베이스 저장 경로 (기본값: Vault 내부 .obsidian-rag/)
# 보통은 변경할 필요 없습니다.
# DB_PATH=/custom/path/to/databases

# 로그 레벨 (기본값: INFO)
# 디버깅이 필요하면 DEBUG로 변경
# LOG_LEVEL=DEBUG

# 청크 크기 (기본값: 1000)
# 노트를 몇 글자 단위로 나눌지 결정
# 너무 작으면 컨텍스트 손실, 너무 크면 검색 정확도 하락
# CHUNK_SIZE=1000

# 청크 오버랩 (기본값: 200)
# 청크 사이의 중복 글자 수
# CHUNK_OVERLAP=200
