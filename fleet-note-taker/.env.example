# ============================================
# Fleet Note Taker 환경 변수 설정
# ============================================
#
# 이 파일을 복사해서 `.env` 파일을 만들고
# 아래 값들을 여러분의 환경에 맞게 수정하세요.
#
# 복사 방법:
#   cp .env.example .env
#
# ============================================

# Obsidian Vault 경로 (필수)
# Fleet 폴더가 있는 Vault의 루트 경로
#
# 확인 방법:
#   1. Finder에서 Vault 폴더 우클릭
#   2. "Option" 키를 누른 상태에서 "... 경로 이름 복사" 선택
#   3. 아래에 붙여넣기
#
VAULT_PATH=/Users/yourname/Desktop/SecondBrain

# 입력 폴더 경로 (필수)
# 이미지 파일 또는 마크다운 파일을 여기에 추가하면 자동 처리됩니다.
#
# 지원 형식:
#   - 이미지: .jpg, .jpeg, .png, .bmp, .tiff
#   - 마크다운: .md, .markdown (YYMMDD 제목.md 형식)
#
ORIGINAL_NOTES_DIR=/Users/yourname/agents/memo2fleet

# ============================================
# 선택 사항 (고급 사용자용)
# ============================================

# 처리된 이미지가 이동할 경로
# 기본값: VAULT_PATH/99 Fleet/linked_notes
# 보통은 변경할 필요 없습니다.
# LINKED_NOTES_DIR=/Users/yourname/Desktop/SecondBrain/99 Fleet/linked_notes

# 생성된 Fleet Note가 저장될 경로
# 기본값: VAULT_PATH/99 Fleet
# 보통은 변경할 필요 없습니다.
# GENERATED_NOTES_DIR=/Users/yourname/Desktop/SecondBrain/99 Fleet

# Claude CLI 경로
# 기본값: 자동 탐지 (which claude)
# 수동 지정이 필요한 경우에만 주석 해제
# CLAUDE_CLI_PATH=/opt/homebrew/bin/claude
